## 2024-05-23 - [Icon-Only Buttons]
**Learning:** Icon-only buttons (like 'Favorite') are invisible to screen readers without explicit `aria-label` attributes.
**Action:** Always pair icon-only buttons with `aria-label` and `title` attributes. Use dynamic labels (e.g., "Add to favorites" vs "Remove from favorites") for toggle states.
