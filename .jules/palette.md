# Palette Journal

## 2026-01-14 - [GameCard Accessibility]
**Learning:** `group-focus-within` is essential for nested interactive elements where parent hover effects (like revealing an info box) should also trigger on child focus.
**Action:** Always pair `group-hover` with `group-focus-within` when the revealed content contains focusable elements.
