## 2025-05-18 - Toggle Buttons & Accessibility
**Learning:** Toggle buttons (like Favorites) require more than just visual state changes; they need `aria-pressed` and dynamic `aria-label`/`title` to convey state to screen readers and mouse users effectively.
**Action:** Always include `aria-pressed` on toggle buttons and ensure focus states are visible, especially on complex backgrounds.
