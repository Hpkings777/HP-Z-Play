## 2025-12-18 - [Accessible Toggles]
**Learning:** Custom toggle switches built with `<button>` are often read as simple buttons by screen readers, hiding their on/off state.
**Action:** Always add `role="switch"`, `aria-checked`, and a descriptive `aria-label` to custom toggle components.
